# 服务器域名
DOMAIN=herald.your-domain.com

# JWT 密钥（必须更换为随机值，至少 32 字符）
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# 端口
PORT=3000

# 配对码有效期（秒）
PAIR_CODE_TTL=300

# SQLite 数据库路径
DB_PATH=./herald.db
